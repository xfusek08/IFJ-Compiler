
function add(n as integer , p as integer) as integer
  return n + p
end function

Scope 'Hlavni telo programu
  dim i as integer = 0
  dim a as double = 0
  for i = 0 to 20
    a += i
  next
  print a; !"\n";

  /'
  print !"Piste string: \n";
  dim text as string = !""
  do while true
    dim a as string = !""
    input a
    if a = !":q" then
      exit
    end if
    text = text + a
    text = text + !"\n"
  loop
  print !"Napsal jste: \n"; text; !"\n";
  '/
End Scope